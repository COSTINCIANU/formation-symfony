{% extends 'base.html.twig' %}

    {% block title %}
       Bienvenue sur le meilleur réseau d'appartements
    {% endblock %}

{% block body %}

<div class="container">
     <div class="jumbotron mt-5">
        <h1 class="display-3">Bienvenue sur SymBnB !</h1>
        <p class="lead">Nous mettons en relations les voyageurs et 
        les proriétaires qui veulent profiter d'opportunités et gagner
        de l'argent en proposant leur bien à la location !</p>
        <hr class="my-4">
        <p>Vous êtes voyageur ? Et vous voulez voir où 
        vous pourrez loger pour prochain déplacement ?</p>
        <p class="lead">
            <a class="btn btn-primary btn-lg" href="#" role="button">Voir nos annonces</a>
        </p>
     </div> {# fin div jumbotron #}
     
     <h2 class="h1 my-5">Nos appartements stars</h2>

     <div class="row">
         {% for data  in ads %}
            {% include 'ad/_ad.html.twig' with {'ad' : data.annonce } %} 
         {% endfor %}
     </div> {# fin  row  Nos appartements stars #}

     <h2 class="h1 my-5">Nos proriétaires stars !</h2>

     <div class="row">
       {% for data in users %}
        {# la premier card col-md-6 #}
        <div class="col-md-6">
            <div class="card border-secondary mb-3">
                <div class="card-body">
                    <h4 class="card-title">{{data.user.fullName}}</h4>

                    <hr>

                    <div class="card-text">
                        <img src="{{data.user.picture}}" alt="Avatar de {{data.user.fullName}}" class="float-left mr-3">{# mr-3 pour margin right #}
                           <p>{{data.user.introduction}}</p>
                               <a href="{{ path('user_show', {'slug' : data.user.slug}) }}" class="btn btn-success float-right">En savoir plus!</a>
                    </div> {# fin div card-text #}
                </div> {# fin card-body #}
            </div> {# fin div card border-secondary pour une culeur grise et mb-3 pour se metre une en decu de l'autre sur les mobile avec de marge #}
        </div> {# fin col-md-6 pour avoir 2 colone ou col se automatique 2 colone #}
       {% endfor %}
     </div> {# fin div row #}

</div> {# fin div container #}
{% endblock %}